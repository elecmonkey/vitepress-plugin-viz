# 更新日志

## 1.0.0

- **性能优化**：复用 WASM 实例，防止内存泄漏并提升渲染速度。
- **修复**：解决 Windows 系统下的路径兼容性问题。
- **修复**：增强自动组件注入的健壮性，增加错误处理。

## 1.0.0-beta.8

- 支持在 `viz` 代码块中使用 `{scale}` 参数。
- SVG 超出容器宽度时自动等比缩小。

## 1.0.0-beta.3-7

- 修复仓库发布工作流。

## 1.0.0-beta.2

- 支持深色模式。

## 1.0.0-beta.1

- 支持在 Markdown 中使用 `viz` 代码块。
