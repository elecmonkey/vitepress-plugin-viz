# 开发指南

本项目使用 `pnpm` 进行包管理。请确保您已安装 [Node.js](https://nodejs.org/) (推荐 v24+) 并启用 `corepack`。

启用 corepack 以使用正确版本的包管理器：

```bash
corepack enable
```

安装项目依赖：

```bash
pnpm i
```

启动文档开发服务器以预览您的更改：

```bash
pnpm docs:dev
```

## 参与贡献

非常欢迎您的贡献！请随时提交 Pull Request。
